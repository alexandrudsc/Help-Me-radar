<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="@dimen/activity_vertical_margin"
    tools:context="com.developer.alexandru.helpmeradar.ContactsActivity">

    <LinearLayout
        android:id="@+id/btn_type_new_contact"
        android:layout_alignParentBottom="true"
        android:background="@drawable/button_type_contact"
        android:layout_width="match_parent"
        android:layout_height="@dimen/type_contact_button">

        <TextView
            android:text="@string/text_btn_type_new_contact"
            android:textSize="@dimen/medium_text"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
    </LinearLayout>

    <ListView android:id="@android:id/list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/btn_type_new_contact"/>

    <TextView android:id="@android:id/empty"
        android:text="@string/no_contacts"
        android:layout_centerInParent="true"
        android:textSize="@dimen/gps_coord_size"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>


</RelativeLayout>
